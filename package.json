{
  "name": "immi-case",
  "type": "module",
  "scripts": {
    "test:workers": "vitest run --config workers/vitest.config.js",
    "test:sql": "python3 scripts/sql-harness/verify-rebuild-sql.py"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "wrangler": "4.120.0"
  }
}
